[build]
builder = "nixpacks"
buildCommand = "pip install -r trader_koo/requirements.txt && pip install -e ."

[deploy]
startCommand = "bash start.sh"
healthcheckPath = "/api/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
