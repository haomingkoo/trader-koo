[build]
builder = "nixpacks"
buildCommand = "pip install -r trader_koo/requirements.txt"

[deploy]
startCommand = "uvicorn trader_koo.backend.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/api/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
